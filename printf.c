#include "main.h"
#include <stdio.h>

/**
 * _printf - function to be used to handle formats
 * @format: format of data to be printed
 * Return: numbers of elements in a string
 */


int _printf(const char *format, ...)
{
	char x = x;
	char *str = "hope";
	{
		printf("%c\n", x);
		return (x);
	}

	printf("%s\n", str);
	printf("%\%\n");

	return (0);
}
